# Used by docker-compose.yaml to provision the local Postgres container.
POSTGRES_USER=scheduling
POSTGRES_PASSWORD=scheduling
POSTGRES_DB=scheduling_platform
POSTGRES_PORT=5433

# Matches the values above — used by backend/.env (DATABASE_URL)
# postgresql://scheduling:scheduling@localhost:5433/scheduling_platform
